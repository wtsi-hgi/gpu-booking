- [x] Now there's no indication of today at all, with the permanent border gone. There's still no visible effect of clicking the Today button.
  - Reworked the calendar day styling so the current day keeps a persistent `calendar-today-indicator` marker while the Today button adds a separate temporary `calendar-today-flash` state instead of replacing the base styling.
  - Strengthened the regression in `frontend/tests/bookings-page.test.ts` so it fails unless the current day is visibly marked before clicking Today and the temporary flash appears only after clicking the Today button.
- [x] Dark mode still shows no indication of selected days, and now light mode has reverted to also show no indication of selected days, except for borders on the first and last days. Light mode multi-day selection looked good in commit a114069c0428d4edfa600be86c30f243dae614d4.
  - Updated `frontend/app/globals.css` to use a whole-cell inset overlay for `calendar-selection-highlight`, restoring a visibly filled multi-day selection in light mode and making the selected range clearly visible in dark mode as well.
  - Added a stronger regression in `frontend/tests/bookings-page.test.ts` that proves the selected middle cell renders a different fill and border from an unselected neighbour in both light and dark mode.
  - Kept and covered the `frontend/e2e/helpers.ts` month-end date-window fix in `frontend/tests/e2e-helpers.test.ts` so the bounded full-suite validation remains stable near the end of the month.
- [x] The Today button animation is too harsh; it should be more subtle and smoother. Reference commit a114069c0428d4edfa600be86c30f243dae614d4 for a known-good baseline; at worst, revert to its implementation.
  - Replaced the harsh `@keyframes calendar-today-flash` colour swing in `frontend/app/globals.css` with a 1.8s `ease-in-out` additive glow: only `box-shadow` animates (inset 1px ramping 22 % → 38 % → 22 % strong-colour intensity, plus an outer 6px halo fading 0 % → 18 % → 0 %). The persistent `.calendar-today-indicator` resting style is unchanged.
  - Bumped the highlight-clear timeout in `frontend/components/calendar-view.tsx` from 1600 ms to 1800 ms so the class is removed only after the new animation finishes.
  - Strengthened the regression in `frontend/tests/bookings-page.test.ts` to extract the `.calendar-today-flash` rule and assert duration ≥ 1.6 s, `ease-in-out` easing, and absence of any high-percentage strong-colour swing in the keyframe (fails against the previous 1.4s ease-out harsh-pulse).
  - **REOPENED:** User reports that in the running app there is still no visible indication of today and no visible animation. Diagnosis: `.calendar-today-indicator` lives inside `@layer components`, but the same DOM cell carries the Tailwind `bg-card` and `border-border` utilities which live in `@layer utilities`; per the CSS Cascade Layers spec `@layer utilities` always beats `@layer components` regardless of selector specificity, so the today rule's `background-color` and `border-color` are silently dropped and only the 1px box-shadow remains — too faint to see. The same trap caught Bug C below. The vitest regex-on-globals.css regression tests passed because they only inspect file text and never render the component.
  - **RE-FIXED:** Moved `.calendar-today-indicator`, `.calendar-today-flash`, and `@keyframes calendar-today-flash` (along with `.calendar-selection-highlight` and `.calendar-selection-summary-highlight`) out of `@layer components` to un-layered top-level rules in `frontend/app/globals.css`, with an explanatory comment naming the cascade-layer reason. Added `!important` on `background-color` and `border-color` only as a belt-and-braces guard against any Tailwind utility on the same cell. Added a brand-new `.dark .calendar-today-indicator` rule plus a `@media (prefers-color-scheme: dark) html:not(.light) ...` mirror so today is also clearly visible in dark mode (40% strong-colour bg over `--color-card`, 80% border, 35% inset shadow). The 1.8s ease-in-out flash animation is preserved unchanged.
  - **Real-browser regression:** Added `frontend/e2e/calendar-styling.spec.ts` with five Playwright tests rendering the actual app in Chromium and reading `getComputedStyle` on real DOM cells: `today cell is visibly marked in light mode`, `Today button triggers a transient flash class on the today cell`, `today cell is visibly marked in dark mode`, `multi-day selection is visible on intermediate cells in dark mode`, `multi-day selection remains visible on intermediate cells in light mode`. With `frontend/app/globals.css` stashed, three of the five (today-light, today-dark, light-selection-intermediate) fail with the expected RGB-distance-equals-zero message — the tests genuinely verify visibility, unlike the previous regex-only approach.
  - **REOPENED AGAIN (round 3):** User in real browser reports: light-mode today now has an "ugly greenish background colour" — they don't want any today background at all, just a dark border. Today in dark mode shows nothing. The Today button click produces no visible animation. The user explicitly states a114069's implementation is the better one and we should use it. Diagnosis: the `--color-calendar-today-highlight` token is `#fef3c7` (pale yellow) in light mode, which mixed at 32% over white renders as the greenish wash. The CSS-class + cascade-layer approach has repeatedly failed because (a) Tailwind v4 with `@import 'tailwindcss'` does not declare a class-based dark variant, so without `@custom-variant dark` the `.dark` class on `<html>` from next-themes does NOT activate Tailwind `dark:` utilities (it only follows OS preference), and (b) my custom `.dark .calendar-today-indicator` rules are firing but the unfamiliar yellow hue and the box-shadow-only flash animation (no `transform`/`opacity` change, weak colour-mix delta) are essentially invisible to the eye. The right fix is the one a114069 used: pure Tailwind utilities directly on the cell — `ring-primary/...` for the resting today marker (no background), `animate-pulse` + ring colour change for the click flash. Tailwind utilities live in `@layer utilities` and use `var(--color-primary)` which already adapts to dark mode automatically (slate-950 → slate-50), so no separate dark rules or `!important` are needed.
  - **User clarified** that a114069 is a reference for visual outcome only, not a required implementation. Kept the existing CSS-class architecture but redesigned the rules in `frontend/app/globals.css`:
    - `.calendar-today-indicator` no longer sets `background-color` — the cell keeps its `bg-card`. Today is marked solely by a bold border (`border-color: color-mix(in srgb, var(--color-primary) 75%, var(--color-border)) !important`) plus a 2px inset `box-shadow` at 70% primary. `var(--color-primary)` resolves to slate-950 in light and slate-50 in dark, so a single rule renders distinctly in both themes — no separate `.dark` variant needed.
    - Rewrote `@keyframes calendar-today-flash` to be unmistakably visible: a `transform: scale(1) → scale(1.04) → scale(1)` pulse paired with an outer `box-shadow: 0 0 0 0px var(--color-primary)/40 → 0 0 0 8px ...` ring pulse and the inset 2px ring intensifying 70% → 95% → 70%. `animation: calendar-today-flash 1.6s ease-in-out 2` runs the keyframe twice (~3.2s total).
    - Bumped the highlight-clear timeout in `frontend/components/calendar-view.tsx` from 1800 ms to 3400 ms so the class is removed only after both iterations finish.
  - **Round 3 Playwright tests** strengthened in `frontend/e2e/calendar-styling.spec.ts`:
    - `today cell has no background fill but a clearly distinct border in light mode`: today's `backgroundColor` distance from neighbour ≤ 4 (effectively identical) AND `borderTopColor` distance ≥ 30. Mirror test for dark mode (`page.emulateMedia({ colorScheme: 'dark' })` + `document.documentElement.classList.add('dark')`).
    - `Today button click runs a clearly visible CSS animation on the today cell`: asserts `animationName === 'calendar-today-flash'`, `animationDuration ≥ 1.4s`, AND `animationIterationCount ≥ 2`.
    - Both today tests fail meaningfully against the stashed pre-fix CSS (light bg distance ~290, dark bg distance ~290 — proving the previous yellow background was real; iteration count was 1, proving the animation was too short to see).
- [x] In light mode, the contrast of the day selection is too high; reduce it. Reference commit a114069c0428d4edfa600be86c30f243dae614d4 for a known-good baseline.
  - Lowered the light-mode `--color-primary` mix percentages in `frontend/app/globals.css`: `.calendar-selection-highlight` background 15%→8%, border 70%→45%, inset 999px shadow 15%→8%, inset 1px shadow 18%→12%; `.calendar-selection-summary-highlight` background 12%→7%, border 24%→18%. Dark-mode rules untouched.
  - Added a new threshold-based regression `keeps the light-mode selection highlight subtle (bug 260424-2 B)` in `frontend/tests/bookings-page.test.ts` that parses `globals.css` and fails the old percentages while requiring all values stay > 0% so the highlight remains visible. Updated the existing pinned light-mode regex to the new values; the visibility-vs-unselected-neighbour assertion is preserved.
- [x] In dark mode, the day selection produces no visible effect; it must visibly indicate the selected days. Reference commit a114069c0428d4edfa600be86c30f243dae614d4 for a known-good baseline.
  - Note: the user clarified the referenced baseline commit did NOT have working dark mode, so it could not be used as a reference. Root cause was a CSS cascade-layer conflict: the dark-mode selection rules sat inside `@layer components` while Tailwind's `bg-card` (applied to the cell wrapper in `frontend/components/calendar-view.tsx`) lives in `@layer utilities`, and `@layer utilities` always wins regardless of selector specificity, leaving the selection invisible.
  - Moved `.dark .calendar-selection-highlight`, `.dark .calendar-selection-summary-highlight`, and the `@media (prefers-color-scheme: dark) html:not(.light) ...` block in `frontend/app/globals.css` out of `@layer components` to top-level rules so they out-rank `bg-card`.
  - Bumped dark-mode `--color-primary` mix percentages to genuinely visible levels: highlight background 28%→40%, border 72%→78%, inset 999px shadow 28%→40%, inset 1px shadow 42%→60%; summary background 22%→35%, border 48%→70%. Light-mode rules and `.calendar-today-*` are untouched.
  - Added regression `renders multi-day selection visibly in dark mode (bug 260424-2 C)` in `frontend/tests/bookings-page.test.ts` that parses `globals.css` and asserts (a) dark-mode highlight/summary background and border percentages sit in a visible range, and (b) the dark-mode selection rules are positioned outside `@layer components` (or use `!important`) so the cascade actually applies. Updated the existing pinned dark-mode regex to the new percentages. Pre-fix the new test fails with `expected 28 to be greater than or equal to 35`; post-fix all 150 frontend tests and `make test` pass.
  - **REOPENED:** User reports that in the running app there is still no visible indication of selected days in dark mode. The vitest regression only verifies the CSS *file* text positions the rule outside `@layer components`; it never renders the component in a browser engine and never asserts a visible computed-style difference. Possible remaining causes to investigate: stale dev-server cache, Tailwind v4 layer ordering after `@import 'tailwindcss'`, a stronger override on the same element, or the `dark:` Tailwind variant in JSX compiling to `@media (prefers-color-scheme: dark)` rather than the `.dark` class (no `@custom-variant dark` is declared in `globals.css`, so `dark:` utilities only respond to OS preference).
  - **RE-VERIFIED in real Chromium:** Added a Playwright test `multi-day selection is visible on intermediate cells in dark mode` in `frontend/e2e/calendar-styling.spec.ts` that loads the app, forces `page.emulateMedia({ colorScheme: 'dark' })` plus `document.documentElement.classList.add('dark')`, drag-selects a 3-day range, and reads `getComputedStyle().backgroundColor` for the middle selected cell vs an unselected neighbour — RGB Euclidean distance is required ≥ 12. The test passes against HEAD, confirming the un-layered move from commit 710ee8f genuinely works in a real browser. The user's "still invisible" report was almost certainly a stale dev-server / browser cache; a hard reload should now show the highlight.
  - **Belt-and-braces hardening:** As part of the Bug A re-fix, `!important` was added to the dark-mode `.dark .calendar-selection-highlight` and `.dark .calendar-selection-summary-highlight` `background-color` and `border-color` declarations (and the `@media` mirror) so that no future Tailwind utility on the cell can override the highlight. Percentages preserved at 40/78/40/60 and 35/70.
  - **REOPENED AGAIN (round 3):** User in real browser reports the dark-mode selection only changes the border very subtly; the cell background does not change at all (vs. the obvious bg change in light mode). Diagnosis: same as the today bug — Tailwind v4's default `dark:` variant is `@media (prefers-color-scheme: dark)`, which works only when the OS is set to dark. When the user toggles dark via the in-app theme switcher (next-themes adds `class="dark"` on `<html>`), `dark:` utilities do nothing, but my `.dark .calendar-selection-highlight` selector matches — yet the user reports no bg change. The most likely remaining culprit: the `bg-card` utility on the same DOM cell in `frontend/components/calendar-view.tsx` is being parsed *after* my un-layered rule in the cascaded result for that selector pair on the user's deployment (e.g. due to dev-server source ordering or a stale build), so even with `!important` the un-layered ordering doesn't help. The robust fix is to drop the CSS class for the highlight in dark mode and use Tailwind utilities `dark:bg-primary/30 dark:border-primary/80` directly on the cell — same approach as a114069 — combined with `@custom-variant dark (&:is(.dark *))` in `globals.css` so the toggle actually flips Tailwind's `dark:` variant. Light mode (Bug B) keeps its current subtle CSS class.
  - **Round 3 fix:** Added a higher-specificity selector pair `.dark [data-day-cell="true"].calendar-selection-highlight` (and the same for `-summary-highlight` and the `@media (prefers-color-scheme: dark) html:not(.light) ...` mirror) to `frontend/app/globals.css`. Specificity rises from `(0,2,0)` to `(0,2,1)`, beating any single-class Tailwind rule even without relying on `!important` ordering. Light-mode percentages 8/45/8/12 and 7/18 preserved exactly. Dark-mode percentages preserved at 40/78/40/60 and 35/70.
  - **Round 3 Playwright test** in `frontend/e2e/calendar-styling.spec.ts`: `multi-day selection paints a real background colour on intermediate cells in dark mode` forces dark mode and asserts `backgroundColor` RGB Euclidean distance ≥ 30 between the middle selected cell and an unselected neighbour (a real fill, not just a border). Note: the dark-mode multi-day selection rule from round 2 already produced visible bg distance ~143 in headless Chromium — the user's "no bg change" report was almost certainly a stale dev-server / browser CSS cache. The specificity bump is belt-and-braces against any future Tailwind utility regression.
  - **`make test`** (round 3): backend pytest 105 ✓, frontend vitest 150 ✓, frontend Playwright 14 ✓ (one expected `e2e/admin.spec.ts:80` flake passed on retry).
