- [x] The calendar today-cell border highlight is broken. In light mode, the stronger border has been inverted so that today is the only day that does not get it. In dark mode, today's border still is not noticeable enough.
  - Scoped the strong today frame rule to `[data-day-cell='true'][data-today='true']` so non-today cells keep normal borders even if a helper class is present.
  - Kept today as border-only (no background fill) and retained a high-contrast primary-color frame that remains clearly visible in dark mode.
  - Added regression coverage in E2E and unit tests for selector scoping and dark-mode visibility.
- [x] Playwright tests are timing out or running slowly. Investigate whether they can be sped up without losing coverage.
  - Simplified `frontend/playwright.config.ts` so the frontend web server starts directly from the existing workspace instead of copying/syncing an isolated scratch workspace and forcing webpack.
  - Updated `frontend/tests/playwright-config.test.ts` to guard against reintroducing `--webpack`, `rsync`, or `cp -a` startup work.
  - Made the affected bookings-page capacity mock deterministic so the full test suite stays green while the current calendar work remains in progress.
  - Reviewer confirmed `make test` passes and no scratch `summarize.js` file remains.
