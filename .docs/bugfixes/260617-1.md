- [x] Rework how bookings are done and how admins specify what is available. Instead of “GPU Type”, “GPU Count”, “GRAM”, “System Memory”, users can only reserve entire GPU hosts. Those 4 booking fields become just “GPU Host Type” and “Host Count”. Admin only has to specify count of each type of host. A type of host consists of GPU type (eg. H100) and GPU count (eg. 8), forming a single host entity with an auto-generated name like “8 GPU H100” (easily tweakable in the frontend based on feedback, not stored in db), which is the string that is selectable in the “GPU Host Type” booking form dropdown. Adjust restriction rules accordingly. Don’t implement any backwards compatability, schema migrations or even leave comments about the old way things were done.
  - Replaced the backend booking/capacity model with `GpuHostType` plus booking `gpu_host_type_id` and `host_count`, and updated APIs, schemas, seed data, services, and backend tests to validate capacity by whole host counts.
  - Reworked frontend contracts, server actions, booking/admin forms, booking tables, and admin navigation around GPU Host Type and Host Count; generated labels such as `8 GPU H100` in frontend presentation code only.
  - Removed obsolete GPU type and memory/GRAM admin pages, components, tests, and stale `.docs/booking` planning docs; updated `README.md` to describe the host-booking model directly.
  - Added host-booking regression coverage including `backend/tests/test_host_booking_regression.py`, host-type frontend action/page tests, migrated Vitest suites, and updated Playwright admin/booking flows while preserving calendar visual regressions.
  - Reviewer PASS after `timeout 1200s make lint`, `timeout 1200s make format-check`, and `timeout 1200s make test` all passed; final full test gate reported backend pytest 101 passed, frontend Vitest 143 passed, and Playwright 14 passed.
- [ ] Add or update frontend Playwright coverage for the host-booking changes using the existing setup if it is already good. Test with chromium; no need to make the setup more like `~/wa` unless a real gap appears.
- [ ] Investigate why backend/gpu_booking.db is committed and consider if it should be removed.
