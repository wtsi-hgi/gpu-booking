- [x] Today is once again highlighted with a border. It's a bit subtle in light mode and extremely sublte in dark mode. Both could be a bit better.
  - Strengthened `.calendar-today-indicator` in `frontend/app/globals.css`: `border-color` now uses `var(--color-primary)` solid (was 75% mix with `--color-border`); `box-shadow` now `inset 0 0 0 3px var(--color-primary), 0 0 0 1px var(--color-primary)` (was 2px @ 70%). Single rule serves both themes because `var(--color-primary)` flips slate-950 ↔ slate-50.
  - Added `[data-day-cell='true'] { border-color: var(--color-border) }` to defeat Tailwind v4 `@theme inline` literal-inlining of `border-border` (which was freezing the neighbour cell border at slate-200 in dark mode, leaving today's slate-50 border with almost no contrast). Order: this cell-default rule sits before `.calendar-today-indicator`, whose `!important` wins.
  - Bumped the Playwright real-browser thresholds in `frontend/e2e/calendar-styling.spec.ts` for `today cell ... in light mode` and `... in dark mode` from `borderTopColor` distance ≥ 30 to ≥ 300. Stash-fail confirmed: pre-fix light distance 267, dark distance 49 (both fail); post-fix light 357, dark 318 (both pass).
  - Updated regex assertions in `frontend/tests/bookings-page.test.ts` to match the new rule body.
  - `make test`: backend 105 ✓, vitest 150 ✓, Playwright 14 ✓. Animation code, selection rules (light 8/45/8/12, 7/18; dark 40/78/40/60, 35/70), and lint state unchanged.
- [ ] There's still no visible animation on clicking the Today button. After 4 or 5 attempts at this, it seems like this might be beyond LLM capability. Unless you have any other ideas on why it isn't working, just remove all code related to trying to animate a highlight and leave it as a permanent border on today.
