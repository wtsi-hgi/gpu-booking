- [x] Clicking on month in Calender view opens a drop down to select month, but the left column of months is truncated off the left of the window. Instead of centering the months box under the clicked month, the box should be left aligned to the left of the clicked month.
	- Added a regression assertion in frontend/tests/bookings-page.test.ts that the month selector anchors with left-aligned positioning instead of centered translate-based positioning.
	- Updated frontend/components/calendar-view.tsx to anchor the month selector popover to the trigger's left edge.
- [ ] The “Today” button animation for highlighting current day doesn’t work. The current day has a permanent border colour and there’s no apparent effect or animation visible on clicking the Today button. Maybe try pulsing the background colour of the day box a pastel yellow. Get rid of code related to prior failed attempts at achieving this.
- [ ] In dark mode, the highlight of selected days in calendar views is not visible. There have been prior attempts at fixing this, but they have failed (A failed attempt at fixing this was already attempted (see`.docs/bugfixes/260422-1.md`). It’s not a sublte “not really visible” issue, it’s a “makes absolutely no visible difference” issue. I think the previous attempt only made the selection highlight in light mode uglier, so revert that change.
- [ ] When there is no selection, the “Selection details” box has a box that says “Choose a single day or drag across several days to preview the best-fit booking window without leaving this page.”. Get rid of this text and sub-box when there is no seleciton.
