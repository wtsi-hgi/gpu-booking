- [x] Clicking on month in Calender view opens a drop down to select month, but the left column of months is truncated off the left of the window. Instead of centering the months box under the clicked month, the box should be left aligned to the left of the clicked month.
  - Added a regression assertion in frontend/tests/bookings-page.test.ts that the month selector anchors with left-aligned positioning instead of centered translate-based positioning.
  - Updated frontend/components/calendar-view.tsx to anchor the month selector popover to the trigger's left edge.
- [x] The “Today” button animation for highlighting current day doesn’t work. The current day has a permanent border colour and there’s no apparent effect or animation visible on clicking the Today button. Maybe try pulsing the background colour of the day box a pastel yellow. Get rid of code related to prior failed attempts at achieving this.
  - Added a regression in frontend/tests/bookings-page.test.ts covering the temporary Today flash and asserting the old persistent border/ring treatment is absent.
  - Updated frontend/components/calendar-view.tsx and frontend/app/globals.css to replace the failed ring pulse with a temporary pastel-yellow background flash.
- [x] In dark mode, the highlight of selected days in calendar views is not visible. There have been prior attempts at fixing this, but they have failed (A failed attempt at fixing this was already attempted (see`.docs/bugfixes/260422-1.md`). It’s not a sublte “not really visible” issue, it’s a “makes absolutely no visible difference” issue. I think the previous attempt only made the selection highlight in light mode uglier, so revert that change.
  - Replaced the mixed `border-primary/bg-primary` selected-day utilities in `frontend/components/calendar-view.tsx` with a dedicated `calendar-selection-highlight` class so the failed light-mode tinting is removed from the component logic.
  - Added the theme-aware selection highlight in `frontend/app/globals.css`, with a stronger dark-mode border/background treatment and a lighter light-mode treatment.
  - Reworked the regression in `frontend/tests/bookings-page.test.ts` so it fails unless the old attempted classes are gone, the dedicated selection highlight is used, and `frontend/app/globals.css` still defines a stronger dark-mode selection treatment instead of a no-op or light-mode-strength rule.
- [x] When there is no selection, the “Selection details” box has a box that says “Choose a single day or drag across several days to preview the best-fit booking window without leaving this page.”. Get rid of this text and sub-box when there is no seleciton.
  - Added a regression in `frontend/tests/bookings-page.test.ts` that clears a selection and asserts the empty-state helper copy is no longer rendered.
  - Updated `frontend/components/calendar-view.tsx` to render no sub-box content when the selection details panel has no active selection.
