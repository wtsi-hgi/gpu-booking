- [x] Attempt a defensive fix for a possible Mac Safari issue where the calendar day “Details” button still displays when the “Selection details” box is shown to the right, even though the issue cannot be reproduced in Chromium or WebKit-on-Linux.
  - Hypothesis: the previous implementation used `xl:hidden` and `xl:pb-2` to hide the selected-day `Details` jump affordance at the same breakpoint where the selection panel moves beside the calendar. If Mac Safari page zoom ever lets the rendered panel sit to the right while that media-query class is not the deciding signal, the button can remain visible.
  - Defensive fix: `frontend/components/calendar-view.tsx` now measures the rendered calendar grid and selection panel rectangles, records `data-selection-layout="beside|below|unknown"`, and renders the selected-day jump button plus extra bottom padding only when the measured layout is `below`.
  - Added/updated coverage: `frontend/tests/bookings-page.test.ts` stubs rendered rectangles to prove the jump affordance remains visible when the panel is measured below and is absent when the panel is measured beside the grid. `frontend/e2e/bookings.spec.ts` now asserts the measured layout marker in the existing Chromium wide-hidden/narrow-visible browser regression.
  - Local reproduction status: still no proven Mac Safari reproduction. Focused Chromium and Linux WebKit checks both pass: right-side panel hides `Details`; below-panel layout shows `Details`.
  - Validation: `timeout 1200s make lint` passed. `timeout 1200s make test` passed with backend 114 passed, Vitest 165 passed, and Playwright Chromium 20 passed.
