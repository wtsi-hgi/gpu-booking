- [x] If admin has set the count of hosts of a type to 0, that host type should not be listed in the booking form “GPU Host Type” dropdown or on the “GPU Host Type” filter dropdown for the calendar (but “All GPU host types” should still show bookings for hosts that used to have a >0 count but now they’re 0).
  - Filtered concrete GPU Host Type dropdown options in the booking form and calendar filter to host types with `total_count > 0`, while preserving the calendar's `All GPU host types` option.
  - Added frontend regressions proving zero-count host types are absent from the booking form and calendar filter, and that historical zero-count host-type bookings still show under All.
  - Reviewer PASS after `timeout 1200s make lint`, `timeout 1200s make format-check`, and `timeout 1200s make test`; full test gate reported backend 101 passed, frontend Vitest 146 passed, and Playwright 14 passed.
- [x] Change the “Host Count” from a free text entry to a number selector, that only allows selecting from 1 to num-currently-bookable-hosts-of-selected-host-type, greyed out until user picks a host type. Where a host type has 0 currently-bookable hosts, it will be greyed out and not selectable in the “GPU Host Type” dropdown. By “currently bookable” I mean num total hosts set by admin minus num hosts of the type used by confirmed bookings, and the minimum such value over the selected date range.
  - Added a host-type availability endpoint/action/contract that reports the minimum currently bookable host count over the selected inclusive date range, matching existing capacity-consuming booking statuses.
  - Reworked the booking form so Host Count is disabled until a host type is selected, clamps to `1..currently_bookable`, and disables otherwise-positive host types that have no currently bookable hosts for the selected dates.
  - Added backend availability tests, frontend contract/action tests, and booking-form regressions for disabled saturated host types, disabled Host Count, count clamping, date-range changes, and tentative/spot capacity semantics.
  - Reviewer PASS after `timeout 1200s make lint`, `timeout 1200s make format-check`, and `timeout 1200s make test`; full test gate reported backend 104 passed, frontend Vitest 152 passed, and Playwright 14 passed.
- [ ] Get rid of the “Capacity checks run automatically before submission.” text from the booking form.
- [ ] Change “Grant Number” text in the booking form to “Cost Code” and make it required.
- [ ] When an admin confirms a booking, they must enter a “reservation name” before save will work, and that reservation name should be displayed on the booking to users.
