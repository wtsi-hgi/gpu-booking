- [x] Martin Pollard: As an Admin I need to be able to make bookings that start in the past
  - Allowed admin users to create bookings with past start dates in both backend validation and frontend booking-form validation, while preserving the future-start restriction for non-admin users.
  - Added backend, frontend unit, page wiring, and Chromium Playwright regressions for admin past-start bookings and non-admin rejection.
  - Reviewer PASS after `timeout 1200s make lint` and `timeout 1200s make test`; full gate reported backend 114 passed, Vitest 162 passed, and Playwright 16 passed.
- [x] Martin Prete: In the admin dashboard, once you navigate down to Manage Bookings, GPU Types, etc there's no way of going back to the dashboard other than clicking "back" in the browser
  - Added a shared admin subpage header with an `Admin Dashboard` link back to `/admin` on Manage Bookings, GPU Host Types, and Workflow Types pages.
  - Added Chromium Playwright coverage that fails when the dashboard return link is absent and verifies navigation back from each admin subpage.
  - Reviewer PASS after `timeout 1200s make lint` and `timeout 1200s make test`; full gate reported backend 114 passed, Vitest 162 passed, and Playwright 16 passed.
- [ ] Martin Prete: From the Manage Bookings, once the drawer is opened, any changes to the from trigger the "Booking updated succesfully" toast, that blocks the CLOSE button.
- [ ] Martin Prete: In the Bookings, using the Calendar view, what's is the "Details" button supposed to do? (when the browser window is wide enough that selection details are displayed to the right of the calendar, the details button should not be shown)
- [ ] Me: When the selections details box is displayed to the right of the calendar, the “Create booking fo selection (x hosts available)” button text is too wide for the button. Simplify the text to just “Create Booking” since the rest of the text is redundant.
