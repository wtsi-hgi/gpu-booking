- [ ] The calendar day “Details” button may still display when the “Selection details” box is shown to the right in Safari/WebKit, especially when zooming out in a relatively narrow browser window.
  - WebKit-on-Linux was available at `/home/ubuntu/.cache/ms-playwright/webkit-2272`, and the focused existing WebKit regression passed with `timeout 1200s env PLAYWRIGHT_BROWSERS_PATH=/home/ubuntu/.cache/ms-playwright pnpm --dir frontend exec playwright test e2e/bookings.spec.ts --grep "hides the calendar selection Details button" --browser=webkit`.
  - Scratch WebKit sweeps found no case where the selection panel was to the right while `Details` was visible: at 1279 CSS px the panel was below and `Details` displayed; at 1280 CSS px and above the panel was right and `Details` was hidden.
  - Zoom-out equivalents such as a 1000px physical window at 78.125% and 75%, plus `deviceScaleFactor=2`, also kept `Details` hidden whenever the panel moved right.
  - Evidence is in `.tmp/agent/bugfix-260618-3-webkit/`, including `measurements.json`, `measurements-dsf2.json`, `zoom-out-equivalent-1000px-at-100pct.png`, and `zoom-out-equivalent-1000px-at-78-125pct.png`.
  - No product fix was applied because the bad state did not reproduce in WebKit-on-Linux.
