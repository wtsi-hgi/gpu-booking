- [x] Something (me running `make test` or `make run`?) altered files in the repo to include some absolute paths. Revert these and stop it happening again.
  - Root cause: `frontend/next.config.ts` allowed `GPU_BOOKING_NEXT_DIST_DIR` and `NEXT_DIST_DIR` to override Next's dist directory, and `frontend/playwright.config.ts` set that override to `.tmp/agent/playwright/next-dev` during E2E runs.
  - Fix: removed the dist-dir override path, stopped Playwright from setting it, and added a regression test covering the config contract so generated files stay relative to `.next`.
- [x] `make test` fails when my `.env` includes `GPU_BOOKING_AUTH_MODE=insecure`:
  ```
  ________________________________________________________ test_settings_support_legacy_environment_variable_names ________________________________________________________
  
  monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7fdb87612290>
  
      def test_settings_support_legacy_environment_variable_names(monkeypatch) -> None:
          """Accept historical unprefixed names while preferring repo-specific ones."""
  
          monkeypatch.setenv("AUTH_MODE", "oidc")
          monkeypatch.setenv("DATABASE_URL", "sqlite+aiosqlite:///./legacy.db")
          monkeypatch.setenv("INITIAL_ADMIN_EMAILS", "legacy@example.com")
          monkeypatch.setenv("BACKEND_PORT", "9100")
  
          settings = Settings(_env_file=None)
  
  >       assert settings.auth_mode == "oidc"
  E       AssertionError: assert 'insecure' == 'oidc'
  E
  E         - oidc
  E         + insecure
  ```
  - Root cause: `backend/config.py` looked up repo-prefixed env vars before legacy aliases, so an existing `GPU_BOOKING_AUTH_MODE=insecure` in the caller environment overrode test-provided legacy values even with `Settings(_env_file=None)`.
  - Fix: reversed alias precedence where both naming schemes are supported and made the regression test explicitly set conflicting prefixed and legacy values.
- [x] In Calendar view, month and year are adjustable with arrows on the left, but the month and year are also stated on the right. Get rid of the reduntant right statement.
  - Root cause: `frontend/components/calendar-view.tsx` still rendered a separate combined month/year header (`March 2026`) alongside the existing month and year controls.
  - Fix: removed the redundant combined title and added a frontend regression assertion in `frontend/tests/bookings-page.test.ts` so the calendar header only exposes the month and year through the left-hand controls.
- [x] Clicking on month in Calender view opens a drop down to select month, but it is only as wide as the word for the month and has 4 rows with mutiple months on each row overlapping each other, making it an unreadable mess. Fix with better width handling.
  - Root cause: the month selector in `frontend/components/calendar-view.tsx` used a cramped fixed 3-column grid and too-small minimum width, so longer month names collided.
  - Fix: sized the selector to its content, raised its minimum width, switched to a 1-column / 2-column responsive layout, and added regression coverage in `frontend/tests/bookings-page.test.ts`.
- [x] The “Today” button animation for highlighting current day doesn’t work. Clicking it simply removes the border on the current day for a few seconds, before returning it. It should do something more like make the border subtely pulse a different colour. The final border colour should also be less of a contrast to other days.
  - Root cause: the highlighted Today state in `frontend/components/calendar-view.tsx` replaced the base current-day border styling and applied `animate-pulse` to the whole cell, so the border appeared to vanish instead of pulsing.
  - Fix: kept a stable lower-contrast current-day border/ring, added a dedicated `calendar-today-pulse` animation in `frontend/app/globals.css`, and strengthened regression coverage in `frontend/tests/bookings-page.test.ts`.
- [ ] In dark mode, the highlight of selected days in calendar views is still not really visible. Nor is the border on the “Selection details” box.
- [ ] There is an uncessary “Showing current and future bookings” box in Bookings Table view.
- [ ] A cancelled booking immediately dissapears from the Table view, but is still visible when switching to Calendar view, but remains gone when switching back to Table view. It only goes away properly from Calendar view after manually refreshing the page. A manual refresh should not be necessary.
