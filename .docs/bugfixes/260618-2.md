- [x] The "Create Booking" button should be greyed out and unclickable when a normal user has selected a date range including dates in the past
  - Passed the current user's admin flag into the calendar and disabled the selection CTA when a non-admin range starts before today, while preserving the existing zero-availability disable rule and admin past-start behavior.
  - Converted the Playwright repro to assert the fixed disabled styling and no navigation, and added bookings-page unit coverage for normal-user disabled and admin-enabled past-range selections.
  - Reviewer PASS after `timeout 1200s make lint` and `timeout 1200s make test`; full gate reported backend 114 passed, Vitest 164 passed, and Playwright 20 passed.
- [ ] The “details” button on calendar days still displays when the “Selection details” box is shown to the right of the calander, and thus doesn’t seem to do anything. It should not display. I replicate this by zooming out in my relatively narrow browser window until the selection details box shows to the right.
  - Reproduce pass could not reproduce the reported bad state in the current tree: at 1000-1279 CSS px the panel is below the calendar and `Details` is visible; at 1280 CSS px and above the panel is to the right and `Details` is hidden.
  - Existing focused E2E `timeout 1200s pnpm --dir frontend exec playwright test --grep "hides the calendar selection Details button"` passed; screenshots captured at `.tmp/agent/bugfix-260618-2/calendar-details-css-1280.png` and `.tmp/agent/bugfix-260618-2/calendar-details-narrow-window-zoomed-out.png`.
  - No fix was applied because the current checked behavior already matches the requested layout condition in Chromium; Firefox probing was blocked because the Firefox Playwright browser is not installed.
- [x] The “Admin Dashboard” has 6 boxes. 3 are clickable, 3 are not. It’s not obvious from a quick glance that the 3 unclickable ones are just information. The “Pending Bookings” box is especially tempting to click on. To avoid the confurion, it would probably be better to integrate the info in to the clickable boxes. Ie. under “Manage Bookings” title have small text summarising nuber of pending and confirmed bookings, under “GPU Host Types” say how many have been configured, and likewise for “Workflow Types”.
  - Reworked the admin dashboard into only three clickable management cards, embedding pending/confirmed booking counts under Manage Bookings, configured host type count under GPU Host Types, and configured workflow type count under Workflow Types.
  - Added component and Chromium Playwright coverage that fails against the old six-box layout by requiring summaries inside the links and asserting standalone info-card headings are absent.
  - Reviewer PASS after `timeout 1200s make lint` and `timeout 1200s make test`; full gate passed after clearing an existing local Next dev lock, reporting backend 114 passed, Vitest 164 passed, and Playwright 20 passed.
