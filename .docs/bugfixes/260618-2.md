- [x] The "Create Booking" button should be greyed out and unclickable when a normal user has selected a date range including dates in the past
  - Passed the current user's admin flag into the calendar and disabled the selection CTA when a non-admin range starts before today, while preserving the existing zero-availability disable rule and admin past-start behavior.
  - Converted the Playwright repro to assert the fixed disabled styling and no navigation, and added bookings-page unit coverage for normal-user disabled and admin-enabled past-range selections.
  - Reviewer PASS after `timeout 1200s make lint` and `timeout 1200s make test`; full gate reported backend 114 passed, Vitest 164 passed, and Playwright 20 passed.
- [ ] The “details” button on calendar days still displays when the “Selection details” box is shown to the right of the calander, and thus doesn’t seem to do anything. It should not display. I replicate this by zooming out in my relatively narrow browser window until the selection details box shows to the right.
- [ ] The “Admin Dashboard” has 6 boxes. 3 are clickable, 3 are not. It’s not obvious from a quick glance that the 3 unclickable ones are just information. The “Pending Bookings” box is especially tempting to click on. To avoid the confurion, it would probably be better to integrate the info in to the clickable boxes. Ie. under “Manage Bookings” title have small text summarising nuber of pending and confirmed bookings, under “GPU Host Types” say how many have been configured, and likewise for “Workflow Types”.
