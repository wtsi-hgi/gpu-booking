- [x] Show the reservation name in calendar view where a booking is listed in the “overlapping bookings” section.
  - Rendered nonblank trimmed reservation names in calendar overlapping-booking rows without adding empty placeholders for bookings without one.
  - Added a calendar selection-panel regression that verifies a reservation name appears inside the overlapping booking item.
  - Reviewer PASS after `timeout 1200s make lint`, `timeout 1200s make format-check`, and `timeout 1200s make test`; full test gate reported backend 113 passed, frontend Vitest 158 passed, and Playwright 14 passed.
- [x] The “Host Count” field of the booking form is too unusual in allowing typing but auto-capping the number typed. Isn’t there a better kind of UI element for selecting from a fixed range of numbers?
  - Replaced the typeable Host Count number input with a disabled-until-ready fixed-range selector populated from `1..num-currently-bookable-hosts`.
  - Updated booking form unit coverage and Chromium Playwright booking flows to select Host Count values from the fixed options.
  - Reviewer PASS after `timeout 1200s make lint`, `timeout 1200s make format-check`, and `timeout 1200s make test`; full test gate reported backend 113 passed, frontend Vitest 159 passed, and Playwright 14 passed.
- [x] Swap “Cost Code” field in the booking form to “Alternate Email” location, so all the required fields are at the top.
  - Moved `Cost Code` into `Alternate Email`'s former top-row slot and moved `Alternate Email` down into the former `Cost Code` slot.
  - Added a booking form label-order regression while preserving `Cost Code` as required and `Alternate Email` as optional.
  - Reviewer PASS after `timeout 1200s make lint`, `timeout 1200s make format-check`, and `timeout 1200s make test`; full test gate reported backend 113 passed, frontend Vitest 160 passed, and Playwright 14 passed.
- [x] When a day has 0 hosts available due to confirmed bookings, the “Create booking for selection” button should be greyed out and not clickable.
  - Disabled the calendar selection CTA whenever the selected range's tightest availability is 0.
  - Added a Chromium Playwright regression for a confirmed booking consuming the only host, asserting disabled styling and no navigation after a forced click.
  - Reviewer PASS after `timeout 1200s make lint`, `timeout 1200s make format-check`, and `timeout 1200s make test`; full test gate reported backend 113 passed, frontend Vitest 160 passed, and Playwright 15 passed.
- [ ] If a user had selected a particular “GPU Host Type” (not “All GPU host types”) to filter the calendar view, if they then bring up the booking form, their selected type should be pre-selected for them.
